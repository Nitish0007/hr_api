# https://docs.railway.com/reference/config-as-code
# Root `Dockerfile` is auto-detected; dockerfilePath is explicit for clarity.

[build]
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/up"
healthcheckTimeout = 30
